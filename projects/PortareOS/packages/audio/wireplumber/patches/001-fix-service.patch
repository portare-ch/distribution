Run wireplumber as root with the environment the system session needs.

Rebased from 0.4.15 onto 0.5.17: upstream changed ExecStart in both units
(-p main-systemwide, -p %i), which is context here, not the change.

--- a/src/systemd/system/wireplumber.service.in
+++ b/src/systemd/system/wireplumber.service.in
@@ -14,9 +14,12 @@
 AmbientCapabilities=CAP_SYS_NICE
 ExecStart=@WP_BINARY@ -p main-systemwide
 Restart=on-failure
-User=pipewire
+User=root
 Environment=PIPEWIRE_RUNTIME_DIR=%t/pipewire
 Environment=GIO_USE_VFS=local
+Environment=XDG_RUNTIME_DIR=/var/run/0-runtime-dir
+Environment=DBUS_SESSION_BUS_ADDRESS=unix:path=/run/dbus/system_bus_socket
+Environment=HOME=/storage
 
 [Install]
 WantedBy=pipewire.service
--- a/src/systemd/system/wireplumber@.service.in
+++ b/src/systemd/system/wireplumber@.service.in
@@ -19,7 +19,7 @@
 AmbientCapabilities=CAP_SYS_NICE
 ExecStart=@WP_BINARY@ -p %i
 Restart=on-failure
-User=pipewire
+User=root
 Environment=PIPEWIRE_RUNTIME_DIR=%t/pipewire
 Environment=GIO_USE_VFS=local
 
